class GitUsersAdvanced {
  GitUsersAdvanced({
    this.login,
    this.id,
    this.avatarUrl,
    this.url,
  });

  String? login;
  int? id;
  String? avatarUrl;
  String? url;
}

enum Type { USER }
